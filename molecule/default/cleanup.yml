---
- name: Cleanup
  hosts: all
  pre_tasks:
    - name: Remote auto-generated rdev.sh script.
      file:
        path: "{{ rdev_con_script_dest }}"
        state: absent
      delegate_to: localhost
