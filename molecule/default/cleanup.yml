---
- name: Cleanup
  hosts: all
  gather_facts: false
  pre_tasks:
    - name: Remote auto-generated rdev.sh script.
      file:
        path: "{{ playbook_dir }}/rdev.sh"
        state: absent
      delegate_to: localhost
